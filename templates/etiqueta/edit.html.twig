{% extends 'base.html.twig' %}

{% block title %}Edit Etiqueta{% endblock %}

{% block contenedor_principal %}
    <h1>Edit Etiqueta</h1>

    {{ include('etiqueta/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('etiqueta_index') }}">back to list</a>

    {{ include('etiqueta/_delete_form.html.twig') }}
{% endblock %}
