{{ form_start(form , {'attr' : {'novalidate' : 'novalidate'}}) }} <!-- con el atributo no validate conseguimos que no se valide por php si no que por symfony-->
    {{ form_widget(form) }}
    <button class="btn">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
